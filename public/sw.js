// This is a basic service worker to make the app installable.
self.addEventListener('fetch', (event) => {
  // We are not adding any offline caching logic here yet.
  // This just makes the PWA installable.
});
